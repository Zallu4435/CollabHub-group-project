import ModerationReports from '../components/ModerationReports';

export default function ModerationPage() {
  return <ModerationReports />;
}

export const metadata = {
  title: 'Moderation & Reports | Posts Admin',
  description: 'Handle community reports and maintain healthy interactions',
};
