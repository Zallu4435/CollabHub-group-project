import TransactionsPayments from '../../components/TransactionsPayments';

export default function TransactionsPage() {
  return <TransactionsPayments />;
}

export const metadata = {
  title: 'Transactions & Payments | Marketplace Admin',
  description: 'Monitor all financial flows and payment transactions',
};
