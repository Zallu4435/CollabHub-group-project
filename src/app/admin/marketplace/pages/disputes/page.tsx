import EscrowDisputes from '../../components/EscrowDisputes';

export default function DisputesPage() {
  return <EscrowDisputes />;
}

export const metadata = {
  title: 'Escrow & Disputes | Marketplace Admin',
  description: 'Handle buyer-seller conflicts and dispute resolution',
};
